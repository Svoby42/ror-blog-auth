<%= content_for(:title, "Domů") %>
<% if logged_in? %>
  Přihlášen jako: <%= current_user.name %>
  <% if is_admin?(current_user) %>
    (Administrátor)
  <% end %>
<% else %>
  Nepřihlášený uživatel
<% end %>