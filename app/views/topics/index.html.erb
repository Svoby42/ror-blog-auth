<% content_for :title, "Kategorie" %>
<%= link_to "Nová rubrika", new_topic_path, class: "btn btn-primary mb-3" if logged_in? %>
<table class="table table-bordered text-white mt-3">
  <thead>
  <tr>
    <th scope="col">Název</th>
    <th scope="col">Popis</th>
    <th scope="col">Vytvořeno</th>
    <th scope="col">Možnosti</th>
  </tr>
  </thead>
  <tbody>
  <%= render @topics %>
  </tbody>
</table>